Handy is a simple to use array parser.

You can combine this with the C-Style string parser.

Example:

[1,2,3,4]

import handy;
import std.stdio;

int main() {
    string[] array_contents = handyArray(contents);

    writeln(array_contents[0]) -> 1

    return 0;
}